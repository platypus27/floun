# Floun 2.0.0 QA Evidence

Status: scripted release-prep checks passed on June 5, 2026; manual Chrome QA must be completed before tag, push, or Chrome Web Store upload.

## Artifact Evidence

- Package path: `floun/release/floun-2.0.0.zip`
- Extension version: `2.0.0`
- Generated by: `npm run package:extension` during Phase 5 verification
- SHA-256: `a3111dd3cdac1913272eedf5c3f3a8dc8f0fd1c29b92624987216b49b5188daf`
- Size bytes: `314659`

Required archive entries:

- `manifest.json`
- `index.html`
- `background.js`
- `assets/`
- `icons/icon_16.png`
- `icons/icon_48.png`
- `icons/icon_128.png`

Artifact safety checks:

- No `.env` entries.
- No QA fixture HTML.
- No raw fixture token values.
- No Gemini API-key-like values.

## Scripted Verification

| Check | Result | Notes |
| --- | --- | --- |
| `npm run release:ready` | Pass | Runs package, artifact, and store checks. |
| `npm run release:check` | Pass | Tests, build, production audit, typecheck, worker syntax. |
| `npm run package:extension` | Pass | Produces `release/floun-2.0.0.zip`. |
| `npm test` | Pass | 14 test files, 39 tests. |
| `npm run build` | Pass | Production Vite build. |
| `npm audit --omit=dev` | Pass | 0 vulnerabilities. |
| `npx tsc --noEmit` | Pass | TypeScript check. |
| `node --check build/background.js` | Pass | Generated worker syntax. |
| `git diff --check` | Pass | No whitespace errors; Git reported Windows line-ending normalization warnings only. |

## Manual Chrome QA

| Scenario | Result | Evidence |
| --- | --- | --- |
| Load `floun/build/` in Chrome extensions | Pending | Record Chrome version and whether the extension loads without errors. |
| Scan `http://127.0.0.1:4174/crypto-readiness.html` | Pending | Confirm JavaScript, token, TLS/header, and certificate sections render. |
| Scan a known HTTPS site | Pending | Confirm TLS and certificate adapter status is visible. |
| Scan an HTTP site | Pending | Confirm certificate lookup warning is shown as unavailable. |
| Attempt unsupported page such as `chrome://extensions/` | Pending | Confirm graceful popup error. |
| Generate PDF report | Pending | Confirm the report downloads and raw token values are absent. |
| Store package built without Gemini key | Pass | `npm run release:artifact` found no Gemini API-key-like values. |

Manual QA default: do not tag, push, upload, or publish until every manual row is marked Pass or tracked as a release-blocking follow-up.
